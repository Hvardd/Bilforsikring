# Local development: copy to .env.local and restart Vite.
# Vercel: set VITE_API_URL to the public HTTPS backend base URL before building.
# This value is included in browser JavaScript. Never put credentials or secrets here.
VITE_API_URL=http://localhost:8080
